@using DonateTo.ApplicationCore.Common
@{
    ViewData["Title"] = "Signed Up!";
}

<div class="container-message">
    <div class="message-box">
        <div class="logo"><img src="~/Images/logotype-black.png"></div>
        <h4>Signed Up!!!</h4>
        @if (User.IsInRole(Roles.Superadmin))
        {
            <a class="btn btn-lg btn-primary btn-block submit-button" href="@Url.Action("Index","Home")">Go to home page </a>
        }
    </div>
</div>