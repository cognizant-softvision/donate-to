@{
    ViewData["Title"] = "Home Page";
}
    <div class="form-card" asp-action="Register">
        <h3 class="mb-3 font-weight-normal card-title">Admin</h3>
        <div class="form-group">
            @Html.ActionLink("Login", "Login", "Account", null, new { @class = "btn btn-lg btn-primary btn-block submit-button" })
        </div>
        <div class="form-group">
            @Html.ActionLink("Register", "Register", "Account", null, new { @class = "btn btn-lg btn-primary btn-block submit-button" })
        </div>
        <div class="form-group">
            @Html.ActionLink("Logout", "Logout", "Account", null, new { @class = "btn btn-lg btn-primary btn-block submit-button" })
        </div>
        <br />
    </div>